"""
Core fuzzing engine sub-package.

Modules:
    engine   — Main fuzzing loop: sequence extension, rendering, execution, logging.
    state    — Mutable campaign state: resource pool, sequence pool, history.
    explorer — Endpoint/sequence selection strategies: BFS, LLM-guided.
"""
