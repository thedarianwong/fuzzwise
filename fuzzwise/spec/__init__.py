"""
Spec parsing and dependency inference sub-package.

Modules:
    parser       — Parse OpenAPI JSON/YAML into internal Endpoint/Parameter models.
    dependencies — Infer producer-consumer dependency edges between endpoints.
"""
