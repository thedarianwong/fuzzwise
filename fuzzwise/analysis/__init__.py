"""
Post-hoc analysis sub-package.

Modules:
    metrics    — Compute all metrics from JSONL logs.
    comparator — Compare Config A vs B vs C results.
    reporter   — Render rich terminal output and markdown reports.
"""
