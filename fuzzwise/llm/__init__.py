"""
LLM integration sub-package (Config B/C).

Modules:
    client  — Ollama client wrapper: prompt, parse, retry.
    prompts — All prompt template strings.
    graph   — LangGraph feedback loop: generate → test → analyze → refine.
"""
